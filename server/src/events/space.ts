export enum SPACE_EVENTS_INC {
    JOIN = "join",
    MOVE = "move",
    LEAVE = "leave"
}



export enum SPACE_EVENTS_OUT {
    SPACE_JOINED = "space-joined",
    MOVEMENT = "movement",
    USER_JOIN = "user-join",
    UNAUTHENTICATED = "unauthenticated"
}